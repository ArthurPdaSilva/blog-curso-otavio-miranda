# Servidor
Comandos para iniciar o site do zero:

```sh
# Instalar todos os pacotes
npm i
# Configure o .env
npm run migrate
# Seed é opcional
npm run seed
# Build
npm run build
npm start #apenas para teste
```
